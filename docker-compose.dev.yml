services:
  solana-validator-ha:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "9090:9090"
    volumes:
      - .:/app
      - /app/vendor
    environment:
      - GO_ENV=development
    command: ["air", "-c", "/app/.air.toml"]
    restart: unless-stopped
